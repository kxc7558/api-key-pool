# API Key 代理池 · 代码审计报告

**审计日期**：2026-09-02
**审计对象**：`C:/Users/32492/WorkBuddy/2026-09-01-11-19-50/api-key-pool/`（server.js 1190 行 / admin.html 654 行 / config.json / 8 个辅助脚本）
**审计方式**：逐文件细读 + 静态检查（node --check、check-config.js）+ 主动安全测试（模拟跨站请求）+ 端到端回归（tests/verify.js，mock 上游）

---

## 一、审计结论摘要

| 级别 | 数量 | 说明 |
|---|---|---|
| 🔴 P0（已修复） | 1 | 浏览器恶意网页可无密码窃取本机全部上游 Key（DNS rebinding / CORS 配置缺陷） |
| 🟠 P1（已修复） | 2 | ① 每日配额按 UTC 零点重置（中国用户应为北京时间）；② `/v1/models` 查询消耗用户调用配额 |
| 🟡 P2（已修复） | 1 | 操作台 adminToken 明文显示（改为密码框） |
| ⚪ 建议项 | 4 | 无 git 仓库、`/stats` 无鉴权脱敏暴露、测试脚本硬编码分发 Key、上游响应体无大小限制 |

**代码质量**：整体良好。零依赖、结构清晰、错误处理完备（uncaughtException/unhandledRejection 兜底不退出）、密钥全链路脱敏（maskKey/maskAccessToken）、前端 XSS 防护到位（所有用户可控数据均过 `esc()`）、bat 脚本编码规范（GBK+CRLF+chcp 936）。核心调度逻辑（fallback 梯队 / 账号级 RPM / 冷却退避）此前已多轮验证。

---

## 二、🔴 P0：跨站来源可窃取全部上游 Key（已修复）

### 漏洞描述

- 服务监听 `0.0.0.0:8787`，`adminToken` 为空 → `adminAllowed()` 仅按「来源 IP 是否为 127.0.0.1」判定（server.js `isLoopbackReq`）
- 顶层 CORS 对所有路径放行 `access-control-allow-origin: *` + `allow-headers: *`（含 OPTIONS 预检）
- `GET /admin/api/config` 返回**完整 config.json（含 8 个商汤 + 1 个 NIM 明文 Key）**

**攻击链**：用户浏览器打开任意恶意网页 → 网页 JS 向 `http://127.0.0.1:8787/admin/api/config` 发请求 → 请求源 IP 是 127.0.0.1（本机浏览器）→ 无 token 判定通过 → CORS 头允许 JS 读取响应 → **恶意网页静默窃取全部上游 API Key**。实测确认：带 `Origin: http://evil.example.com` 的请求返回 **HTTP 200 + content-length 4169（完整配置）**，OPTIONS 预检也返回 `allow-origin: *`。

### 修复（server.js）

1. 新增 `isTrustedOrigin(origin, reqHost)`：无 Origin（Node/curl/脚本）放行；本机来源（127.0.0.1/localhost/::1）放行；外部来源仅当 Origin 主机名与浏览器实际访问 Host 一致（正常远程管理）才放行
2. OPTIONS 预检：**仅代理路径**（`/v1/*`）返回 CORS 头；管理路径预检带外部 Origin 直接 403
3. 路由分派前：所有非代理路径（`/admin/*`、`/stats`、`/`）带外部 Origin 一律 403

### 验证（测试实例 + 主服务双跑）

| 场景 | 修复前 | 修复后 |
|---|---|---|
| 恶意 Origin 读 `/admin/api/config` | 200（完整配置泄露） | **403** ✓ |
| 恶意 Origin OPTIONS 预检 | 204 + allow-origin:* | **403** ✓ |
| 恶意 Origin 读 `/stats` / `/` | 200 | **403** ✓ |
| 无 Origin（Node/curl/daily-report.js） | 200 | **200** ✓ |
| 本机同源（操作台页面 fetch） | 200 | **200** ✓ |
| 代理路径预检（中转站 SDK） | 204 + * | **204 + *** ✓（不受影响） |
| 真实调用 flash / auto | 200 | **200** ✓ |

---

## 三、🟠 P1 修复项

### 1. 每日配额按 UTC 零点重置 → 改为北京时间

`userQuotaCheck` 用 `new Date(now).toISOString().slice(0,10)` 取「今日」——ISO 字符串是 **UTC 日期**，即北京早 8 点就重置配额，与用户「自然日」预期不符。新增 `quotaDayKey()`（now + 8h 再取 UTC 日期），按 UTC+8 零点重置。

### 2. `/v1/models` 消耗用户调用配额 → 只鉴权不计费

`userQuotaCheck` 此前对 `isProxyPath` 内所有路径（含 `GET /v1/models`）记账，用户查一次模型列表就扣一次 RPM/每日次数。改为**仅对话接口**（`/v1/chat/completions`、`/chat/completions`）记配额，`/v1/models` 只校验 Key 有效性。

---

## 四、🟡 P2 修复项

**操作台 adminToken 明文显示**：`renderSettings` 中 adminToken 输入框为普通文本，共享屏幕场景易泄露管理密码。改为 `type="password"`（遮罩显示，值仍可编辑，hint 已说明）。

---

## 五、⚪ 建议项（用户确认后已全部实施，2026-09-02 第二轮）

| # | 事项 | 处理 |
|---|---|---|
| 1 | **无 git 仓库** | 已 `git init`（main 分支）并两次提交（`7828f3f` init、`325a6ba` 跟进）。`.gitignore` 排除 config.json / keys*.txt / .workbuddy / logs / reports / qa_b/h.txt；新增 `.gitattributes` 固定换行（.sh/.js/.md/.json/.html=LF，.bat/.vbs=CRLF）。已验证敏感文件从未进入任何提交 |
| 2 | `/stats`、`/` 无鉴权 | 设 `adminToken` 后两者均要求管理鉴权（本机免密 / 远程 Bearer）；未设时行为不变（向后兼容）。admin.html 的 loadStats、daily-report.js、test.js 均已支持带 token（`POOL_ADMIN_TOKEN`） |
| 3 | test.js / qa_call.sh 硬编码分发 Key | 全部改为环境变量：test.js 用 `POOL_KEY`（无默认值）、qa_call.sh 用 `:?` 强校验 `POOL_KEY` |
| 4 | 非流式响应无大小上限 | 新增 `MAX_UPSTREAM_BODY = 64MB`，超限返回 502 `upstream_too_large`，防异常上游拖垮内存 |

### 第二轮验证（测试实例 8890/8891 + 主服务 8787）

- 无 adminToken：`/stats`、`/` 免鉴权 200 ✓（兼容）
- 有 adminToken：`/stats`、`/` 无凭证/错凭证 401，对凭证 200 ✓；代理接口不受影响 200 ✓
- 带 token 时恶意 Origin 仍 403（双保险）✓；同源 + 对 token 200 ✓
- `tests/verify.js` 端到端 **14/14 通过**（复跑无回归）
- 主服务 8787 已重启生效：/stats 200、恶意 Origin 403、真实调用 flash 200 ✓

---

## 六、回归验证（修复后）

- `node --check`：server.js + 全部脚本 9 个文件全部通过
- `check-config.js`：静态检查通过（2 条提示为有意配置：rpmPerKey+rpmPerAccount 并存）
- `tests/verify.js` 端到端 **14/14 通过**：401 下线 / 轮询分散 / 429 切换 / 并发 20 / SSE 透传 / provider:model 强制指定 / 账号组共享配额 / 统计面板
- 主服务 8787 已重启生效，真实调用 `flash` 返回 200

---

## 七、涉及文件

- `server.js`：isTrustedOrigin、OPTIONS 收紧、路由 Origin 校验、配额口径 ×2、MAX_UPSTREAM_BODY、/stats 与 / 可选鉴权
- `admin.html`：adminToken 密码框、loadStats 带 token
- `test.js` / `daily-report.js` / `qa_call.sh`：分发 Key 与管理 token 改环境变量（POOL_KEY / POOL_ADMIN_TOKEN）
- `.gitignore` / `.gitattributes`：敏感文件排除、换行符固定
- `README.md` / `CONFIG.md`：跨站防护、配额口径、北京时间、/stats 鉴权、环境变量用法同步
